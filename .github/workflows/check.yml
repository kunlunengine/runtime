name: Check Rust

on:
    push:
        branches: [main]
    pull_request:
        branches: [main]
    merge_group:
      types: [checks_requested]

jobs:
    check:
        runs-on: ubuntu-latest
        permissions:
            contents: read
        steps:
            - uses: actions/checkout@v7
              with:
                  persist-credentials: false
            - name: Install Rust
              uses: dtolnay/rust-toolchain@stable
            - name: Cache cargo registry
              uses: actions/cache@v4
              with:
                  path: ~/.cargo/registry
                  key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}
            - name: Cache cargo git
              uses: actions/cache@v4
              with:
                  path: ~/.cargo/git
                  key: ${{ runner.os }}-cargo-git-${{ hashFiles('**/Cargo.lock') }}
            - name: Validate JSC distribution manifest
              run: cargo xtask jsc-manifest validate
            - name: Test JSC artifact tooling
              run: |
                  bash -n distribution/jsc/scripts/*.sh
                  python3 -m unittest discover -s distribution/jsc/scripts/tests -v
            - name: Cache dependencies for offline backend rejection tests
              run: cargo fetch --locked
            - name: Reject invalid backend selections without network access
              run: python3 distribution/jsc/scripts/test_cargo_backends.py
            - name: Build repository tooling
              run: cargo build -p xtask
            - name: Check code formatting
              run: cargo fmt --all -- --check
            - name: Check code linting
              run: cargo clippy -p xtask --all-targets -- -D warnings
            - name: Run tests
              run: cargo test -p xtask

    m0-macos:
        name: M0 macOS (${{ matrix.arch }})
        strategy:
            fail-fast: false
            matrix:
                include:
                    - arch: arm64
                      runner: macos-15
                      uname: arm64
                    - arch: x64
                      runner: macos-15-intel
                      uname: x86_64
        runs-on: ${{ matrix.runner }}
        permissions:
            contents: read
        steps:
            - uses: actions/checkout@v7
              with:
                  persist-credentials: false
            - name: Verify runner architecture
              run: test "$(uname -m)" = "${{ matrix.uname }}"
            - name: Install Rust
              uses: dtolnay/rust-toolchain@stable
            - name: Cache cargo registry
              uses: actions/cache@v4
              with:
                  path: ~/.cargo/registry
                  key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}
            - name: Cache cargo git
              uses: actions/cache@v4
              with:
                  path: ~/.cargo/git
                  key: ${{ runner.os }}-cargo-git-${{ hashFiles('**/Cargo.lock') }}
            - name: Lint system developer backend
              run: cargo clippy --workspace --all-targets --no-default-features --features system-jsc -- -D warnings
            - name: Run workspace tests
              run: cargo test --workspace --no-default-features --features system-jsc
            - name: Run runtime doctor
              run: cargo run -p kunlun-runtime --no-default-features --features system-jsc -- doctor

    miri:
        name: Miri ownership invariants
        runs-on: ubuntu-latest
        permissions:
            contents: read
        steps:
            - uses: actions/checkout@v7
              with:
                  persist-credentials: false
            - name: Install nightly Rust with Miri
              uses: dtolnay/rust-toolchain@nightly
              with:
                  components: miri
            - name: Prepare Miri sysroot
              run: cargo +nightly miri setup
            - name: Check wrapper-owned invariants
              run: cargo +nightly miri test -p xtask jsc_ownership
